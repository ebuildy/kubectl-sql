## 1. Fixtures

- [x] 1.1 Move the pinned Kubernetes OpenAPI v2 document to `internal/adapter/datasources/k8s/testdata/swagger.json` (from the untracked `swagger.json` at the repo root); add a short header note (in the generator or a `testdata/README`) recording the source Kubernetes version.
- [x] 1.2 Create a small generator-test fixture `internal/adapter/datasources/k8s/testdata/swagger_fixture.json` containing: a `Pod`-like definition with `spec`/`status` `$ref`s a few levels deep, an `ObjectMeta`-like definition, a `list` path with `x-kubernetes-group-version-kind` and `x-kubernetes-action: list`, and a deliberately self-referential definition (e.g. `JSONSchemaProps`-style) for the cycle-guard test.

## 2. Generator (`tools/genk8sschema`)

- [x] 2.1 Scaffold `tools/genk8sschema/main.go` (`package main`) with `-in`/`-out` flags; parse the input file into `k8s.io/kube-openapi/pkg/validation/spec.Swagger`.
- [x] 2.2 Implement resource discovery: walk `Paths`, find `get` operations with `Extensions["x-kubernetes-action"] == "list"`, read `Extensions["x-kubernetes-group-version-kind"]`, and derive the resource name from the path's last segment. Produce `(group, version, resource, kind)` tuples.
- [x] 2.3 Build a `(group, version, kind) -> definition name` index from `swagger.Definitions` using each definition's `x-kubernetes-group-version-kind` extension; resolve each tuple from 2.2 to its definition.
- [x] 2.4 Implement `defToFields(defs map[string]spec.Schema, defName string, depth int, visiting map[string]bool) []schema.Field`, mirroring `schema_openapi.go`'s `openAPISchemaToField`/`openAPITypeToFieldType` map-vs-struct-vs-list classification but recursing through `$ref` for object-typed properties. Apply a depth cap (8) and cycle guard via `visiting`; on cap/cycle, emit the field as `FieldTypeObject` with no `SubFields`.
- [x] 2.5 Assemble `map[string][]schema.Field]` keyed by `"<group>/<version>/<resource>"` (empty group for core, e.g. `"/v1/pods"`) from the resolved definitions.
- [x] 2.6 Serialize the map with `encoding/gob`, gzip-compress it, and write it to `<out-dir>/schema_swagger_k8s_standard_resources.bin.gz`.
- [x] 2.7 Write `internal/adapter/datasources/k8s/schema_swagger_k8s_standard_resources.go`: `// Code generated by tools/genk8sschema from <k8s version> swagger.json. DO NOT EDIT.`, package `k8s`, `//go:embed schema_swagger_k8s_standard_resources.bin.gz`, `var swaggerSchemaDataGzip []byte`.

## 3. Runtime loader

- [x] 3.1 Add `internal/adapter/datasources/k8s/schema_swagger_loader.go`: `swaggerSchemaProvider` struct + `newSwaggerSchemaProvider()`.
- [x] 3.2 Implement lazy decode via `sync.Once`: gzip-decompress `swaggerSchemaDataGzip`, `gob`-decode into `map[string][]schema.Field`, cache the result (and any decode error) for reuse.
- [x] 3.3 Implement `swaggerKey(gvr) string` (`"<group>/<version>/<resource>"`) and `Provide(ctx, gvr) ([]schema.Field, error)`: on index lookup hit, return `append(schema.GuaranteedFields(), fields...)`; on miss, return `(nil, nil)`; on decode error, return the error.

## 4. Strategic provider integration

- [x] 4.1 In `internal/adapter/datasources/k8s/schema.go`, instantiate `newSwaggerSchemaProvider()` and call `Provide` right after the default baseline is set on `root`, merging via `mergeSchemas` (same pattern as the existing OpenAPI/sample layers, with debug logging on error/empty).
- [x] 4.2 Confirm ordering: default baseline → swagger snapshot → live OpenAPI v3 → sample, with no change to `mergeSchemas` itself.

## 5. Build wiring

- [x] 5.1 Add a `generate` target to `Makefile` that runs `go run ./tools/genk8sschema -in internal/adapter/datasources/k8s/testdata/swagger.json -out internal/adapter/datasources/k8s/schema_swagger_k8s_standard_resources.go`.
- [x] 5.2 Run `make generate` to produce the real `schema_swagger_k8s_standard_resources.go` and `.bin.gz` asset from the pinned fixture; commit both as generated artifacts.
- [x] 5.3 Remove the stray untracked `swagger.json`, `pods.json`, `chnages.md` from the repo root (superseded by 1.1, or unrelated scratch files) if no longer needed — confirm with the user before deleting anything not produced by this change. (User chose to leave all three files as-is; no deletions made.)

## 6. Tests

- [x] 6.1 Generator unit tests against `swagger_fixture.json`: resource discovery from `paths` (6.1a correct GVK/resource tuples), definition resolution by GVK, `defToFields` recursion depth/structure, cycle guard truncation, deterministic output across repeated runs.
- [x] 6.2 Loader unit tests: known resource (e.g. `pods`) returns guaranteed fields + embedded tree; unknown resource returns `(nil, nil)`; repeated `Provide` calls reuse the decoded index.
- [x] 6.3 `strategicSchemaProvider` test: for a resource covered by the embedded snapshot, `spec`/`status` carry nested structure even when the OpenAPI/sample layers return nothing (e.g. fake/disco clients with no data); for a CRD-style resource not covered, behavior is unchanged from before this change.

## 7. Verification

- [x] 7.1 Run `make lint build` and `make test e2e e2e-run-fake` — all clean.
- [x] 7.2 Manual/envtest check: `kubectl-sql --explain "SELECT spec.containers[0].image, status.phase FROM pods"` shows the nested `spec.containers`/`status.phase` structure resolving correctly, sourced from the embedded snapshot.

## 8. DESCRIBE TABLE schema column

- [x] 8.1 Add `MarshalSubFieldsJSON(fields []Field) (string, error)` to `internal/port/schema/field.go`: recursively encode `[]Field` to JSON as `{"name":, "type":, "subFields":[...]}` per node (omit `subFields` when empty, omit `Path`).
- [x] 8.2 In `internal/domain/commands/query/command.go`, change `runDescribeTable`'s header to `COLUMN`, `TYPE`, `SCHEMA`; for each top-level field where `f.Type.IsObjectLike() && len(f.SubFields) > 0`, set the `SCHEMA` cell to `schema.MarshalSubFieldsJSON(f.SubFields)`; otherwise leave it empty.
- [x] 8.3 Update `openspec/specs/describe-table/spec.md` per the `MODIFIED Requirements` delta in `specs/describe-table/spec.md` (this change) once archived.
- [x] 8.4 Add `internal/domain/commands/query/command_test.go`: `DESCRIBE TABLE pods` against a fake/stub datasource whose schema includes a `spec` field with `SubFields` (e.g. `containers`) — assert the `spec` row's `SCHEMA` cell is valid JSON containing those nested field names, and the `name` row's `SCHEMA` cell is empty.
- [x] 8.5 Run `make lint build` and `make test e2e` — all clean. Manual check: `kubectl-sql "DESCRIBE TABLE pods"` shows a populated `SCHEMA` column for `spec`/`status` and an empty one for `name`/`namespace`.
